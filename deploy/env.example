# Copy to /etc/gli-pft/env or project .env on the server
# Used by systemd unit in deploy/gli-pft-api.service

PYTHONPATH=/opt/gli-pft
ALLOWED_ORIGINS=https://your-domain.com

# Optional: number of uvicorn workers (1 is fine; GLI splines load per worker)
# WEB_CONCURRENCY=2
